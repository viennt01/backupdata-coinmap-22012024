import ConfirmEmail from '@/components/ConfirmEmail';
import { Layout } from '@/components/Layout';

const EmailPage = () => {
  return <ConfirmEmail />;
};

EmailPage.Layout = Layout;

export default EmailPage;
